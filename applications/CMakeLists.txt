# Each subdirectory here is a standalone application.
# No two apps are ever linked into the same binary.
# Add a new app by creating a folder and calling add_baremetal_app() inside it.

add_subdirectory(led-control)
add_subdirectory(i2c-slave)
